requests>=2.31
Pillow>=10.0
reportlab>=4.0
PyMuPDF>=1.24

# Embedded Stable Diffusion (standalone image generation). Install with CUDA support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# (diffusers/transformers/accelerate/safetensors are pulled in automatically)
diffusers>=0.30
transformers>=4.44
accelerate>=0.34
safetensors>=0.4
